import { StrikethroughDirective } from "./strikethrough.directive";

describe("Strikethrough.Directive", () => {
  it("should create an instance", () => {
    expect(new StrikethroughDirective()).toBeTruthy();
  });
});
